<h1><%= @board.board_name %></h1>

<%= link_to "Edit", edit_board_path(@board), class: "ui orange button" %>
<%= link_to "Delete", @board, method: :delete, class: "ui red button" %>
<hr/>
<h2>Click List to View Tasks</h2>
<% @board.lists.each do |list| %>
    <%= list.list_priority %> | 
    <%= link_to list.list_name, board_list_path(@board, list) %> 
    <br />
    <%= link_to "Edit ", edit_board_list_path(@board, list), class: "ui orange button small" %> <%= link_to "Delete", board_list_path(@board, list), method: :delete, class: "ui red button" %>
<br />
<br />
<br />
    <% end %>
<hr/>
<%= link_to "Add List to Board", new_board_list_path(@board) %></div>